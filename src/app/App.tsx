import { Header, RoutesContent } from '../widgets';
import './App.scss';

export default function App() {

  return (
    <div className="App">
      <Header />
      <RoutesContent />
    </div>
  );
}